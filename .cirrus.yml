linux_task:
  name: "cimg/base:edge-20.04"
  arm_container:
    image: cimg/base:edge-20.04
    cpu: 1
    memory: 1G
  timeout_in: 5m
  script:
    - uname -a
    - ls -alrt
    - echo ${PATH} || true
    - which java  || true
    - which sudo  || true
    - which yum   || true
    - which snap  || true
    - which dart  || true
    - which flutter || true
